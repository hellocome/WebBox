
ext.scalaVersion = '2.10'
if (project.name.endsWith('_2.11')) ext.scalaVersion = '2.11'
if (project.name.endsWith('_2.12')) ext.scalaVersion = '2.12'

logger.info "Using Scala version '${scalaVersion}' for project: '$project.name'"

//========================================================
// Specify version numbers used by a number of libraries
//========================================================

ext.versions = [:]

//==== Names that depend on the Scala version
ext.scalaverdepends = [:]

//==== Base Scala version
versions.scala = scalaVersion

//==== Third party
versions.scala_library = '2.10.4'
if (versions.scala == '2.11') versions.scala_library = '2.11.7'
if (versions.scala == '2.12') versions.scala_library = '2.12.1'

versions.scalatest = '2.2.0'
if (versions.scala == "2.11") versions.scalatest = '2.2.4'
if (versions.scala == "2.12") versions.scalatest = '3.0.1'